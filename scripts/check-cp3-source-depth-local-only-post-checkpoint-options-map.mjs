import fs from "node:fs";

const reportPath = "docs/reviews/CP3_SOURCE_DEPTH_LOCAL_ONLY_POST_CHECKPOINT_OPTIONS_MAP_2026-05-29.md";

const requiredPhrases = [
  "Status: CP3 source-depth local-only post-checkpoint options map recorded",
  "REVISE",
  "local-only post-checkpoint options map",
  "CEO-reviewable options",
  "does not approve template copy",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not fill template values",
  "does not create the future evidence checker",
  "does not clear source-depth not_ready",
  "decision governance checkpoint summary is complete",
  "decision governance checkpoint summary role review is complete",
  "completed governance chain means local checks and documentation are complete",
  "completed governance chain does not authorize execution",
  "source-depth production gate remains not_ready",
  "public data source remains mock",
  "Option A: schedule CEO decision meeting",
  "Purpose: decide whether to authorize template-copy approval workflow only",
  "Allowed output: meeting decision record only",
  "Blocked output: real request packet",
  "Blocked output: evidence files",
  "Blocked output: filled template values",
  "Blocked output: future evidence checker",
  "Option B: continue local-only governance hardening",
  "Allowed output: local-only review document",
  "Allowed output: local-only static checker",
  "Allowed output: review gate entry",
  "Blocked output: Supabase connection",
  "Blocked output: SQL execution",
  "Blocked output: market-data fetching",
  "Blocked output: runtime wiring",
  "Option C: pause source-depth and advance UI disclosure readiness",
  "Allowed output: internal UI disclosure plan",
  "Allowed output: non-runtime draft copy policy",
  "Allowed output: blocked-state review checklist",
  "Blocked output: public claims",
  "Blocked output: scoreSource=real",
  "Blocked output: source-depth production transition",
  "Blocked output: public data source real wording",
  "Option D: prepare human approval packet scope only",
  "Allowed output: approval packet scope map",
  "Allowed output: required owner list",
  "Allowed output: stop-condition checklist",
  "Blocked output: actual approval packet",
  "Blocked output: template copy approval",
  "Blocked output: evidence artifact creation",
  "Blocked output: source-rights approval claim",
  "Option A improves decision alignment but requires CEO meeting time",
  "Option B improves local control but delays human authorization",
  "Option C improves product clarity but does not advance source-depth evidence",
  "Option D improves future packet boundaries but does not create the packet",
  "all options keep source-depth not_ready protected",
  "all options keep scoreSource=real blocked",
  "all options keep Supabase blocked",
  "all options keep SQL blocked",
  "all options keep market-data fetching blocked",
  "all options keep public claims blocked",
  "CEO recommendation: Option D first",
  "approval packet scope map reduces ambiguity before any approval packet exists",
  "Next safe slice: record CP3 source-depth local-only approval packet scope map",
  "The next safe slice must remain local-only",
  "The next safe slice must not approve template copy",
  "The next safe slice must not create a real request packet",
  "The next safe slice must not create evidence files",
  "The next safe slice must not fill template values",
  "The next safe slice must not create the future evidence checker",
  "The next safe slice must not connect to Supabase",
  "The next safe slice must not run SQL",
  "The next safe slice must not fetch market data",
  "The next safe slice must not parse market rows",
  "The next safe slice must not wire runtime code",
  "The next safe slice must not set scoreSource=real",
  "The next safe slice must not clear source-depth not_ready",
  "The next safe slice must not make public claims",
  "stop if options map is treated as approval",
  "stop if options map is treated as executable task",
  "stop if options map is used to create a real request packet",
  "stop if options map is used to create evidence files",
  "stop if options map is used to fill template values",
  "stop if options map is used to create future evidence checker",
  "stop if options map is used to connect to Supabase",
  "stop if options map is used to run SQL",
  "stop if options map is used to fetch market data",
  "stop if options map is used to set scoreSource=real",
  "stop if options map is used to clear source-depth not_ready",
  "stop if options map is used to make public claims",
  "scripts/check-cp3-source-depth-local-only-post-checkpoint-options-map.mjs passes",
  "scripts/check-cp3-source-depth-local-only-decision-governance-checkpoint-summary-role-review.mjs passes",
  "scripts/check-cp3-source-depth-local-only-decision-governance-checkpoint-summary.mjs passes",
  "scripts/check-cp3-tw-stock-source-depth.mjs remains not_ready as expected",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "post-checkpoint options map only",
  "do not approve template copy",
  "do not create a real request packet",
  "do not create real evidence artifact files",
  "do not fill template values",
  "do not create future evidence checker",
  "do not add example market data",
  "do not add sample rows",
  "do not add sample JSON",
  "do not add sample CSV",
  "do not add Supabase output",
  "do not add SQL output",
  "do not fetch market data",
  "do not parse market rows",
  "do not run source-depth validator against Supabase",
  "do not connect to Supabase",
  "do not run SQL",
  "do not write Supabase",
  "do not write staging rows",
  "do not write daily_prices",
  "do not create seed SQL",
  "do not store raw market rows",
  "do not commit CSV / JSON market data files",
  "do not set scoreSource=real",
  "do not make public backtest claims",
  "do not clear source-depth not_ready",
  "CP3 source-depth production gate remains not_ready",
  "Keep public data source mock",
  "record CP3 source-depth local-only post-checkpoint options map role review"
];

const forbiddenPhrases = [
  "Status: approved",
  "CEO Decision: APPROVE",
  "Approval Status: approved",
  "template copy approved",
  "real request packet approved",
  "evidence artifacts approved",
  "future evidence checker approved",
  "source_depth_state is reviewable",
  "scoreSource=real approved",
  "source-rights are approved",
  "public claims are approved",
  "Supabase read output:",
  "SQL execution output:",
  "raw OHLCV",
  "daily_prices sample"
];

const report = fs.existsSync(reportPath) ? fs.readFileSync(reportPath, "utf8") : "";
const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      report: reportPath,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
