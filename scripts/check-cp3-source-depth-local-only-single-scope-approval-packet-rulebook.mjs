import fs from "node:fs";

const reportPath = "docs/reviews/CP3_SOURCE_DEPTH_LOCAL_ONLY_SINGLE_SCOPE_APPROVAL_PACKET_RULEBOOK_2026-05-29.md";

const requiredPhrases = [
  "Status: CP3 source-depth local-only single-scope approval packet rulebook recorded",
  "REVISE",
  "local-only single-scope approval packet rulebook",
  "future human approval packet rules",
  "does not approve template copy",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not fill template values",
  "does not create the future evidence checker",
  "does not clear source-depth not_ready",
  "rulebook prepares future CEO review",
  "rulebook reduces bundled approval risk",
  "rulebook is not an approval packet",
  "rulebook is not approval",
  "rulebook is not execution",
  "rulebook is not evidence",
  "source-depth production gate remains not_ready",
  "public data source remains mock",
  "Rule 1: one future approval packet may request exactly one requested scope",
  "Rule 2: one future approval packet must declare explicit non-scope",
  "Rule 3: one future approval packet must name exactly one decision owner",
  "Rule 4: one future approval packet must name required role signoffs",
  "Rule 5: one future approval packet must include stop conditions",
  "Rule 6: one future approval packet must include rollback boundary",
  "Rule 7: one future approval packet must include gate impact",
  "Rule 8: one future approval packet must not bundle unrelated approvals",
  "Rule 9: one future approval packet must not treat prior discussion as approval",
  "Rule 10: one future approval packet must not treat user said continue as approval",
  "Single Scope: blank template copy only",
  "Single Scope: real request packet creation only",
  "Single Scope: request metadata filling only",
  "Single Scope: evidence artifact creation only",
  "Single Scope: future evidence checker creation only",
  "Single Scope: remote read-only validation only",
  "Single Scope: staging migration execution only",
  "Single Scope: source-depth production transition only",
  "Single Scope: scoreSource=real transition only",
  "Single Scope: public claims only",
  "single scope label is not approval",
  "single scope label is not permission",
  "single scope label is not implementation",
  "single scope label is not remote access",
  "single scope label is not public claim clearance",
  "blank template copy only requires CEO owner",
  "real request packet creation only requires CEO owner",
  "request metadata filling only requires CEO owner and PM signoff",
  "evidence artifact creation only requires CEO owner and Legal signoff",
  "future evidence checker creation only requires CEO owner, Engineering signoff, and QA signoff",
  "remote read-only validation only requires CEO owner, Legal signoff, Engineering signoff, and QA signoff",
  "staging migration execution only requires CEO owner, Legal signoff, Engineering signoff, and QA signoff",
  "source-depth production transition only requires CEO owner, Investment signoff, Engineering signoff, and QA signoff",
  "scoreSource=real transition only requires CEO owner, Investment signoff, Marketing signoff, and QA signoff",
  "public claims only requires CEO owner, Marketing signoff, Investment signoff, Legal signoff, and Design signoff",
  "reject packet if it has more than one requested scope",
  "reject packet if requested scope is missing",
  "reject packet if explicit non-scope is missing",
  "reject packet if decision owner is missing",
  "reject packet if required role signoffs are missing",
  "reject packet if stop conditions are missing",
  "reject packet if rollback boundary is missing",
  "reject packet if gate impact is missing",
  "reject packet if raw market rows are included",
  "reject packet if CSV market data is included",
  "reject packet if JSON market data is included",
  "reject packet if Supabase read output is included",
  "reject packet if SQL execution output is included",
  "reject packet if user said continue is treated as approval",
  "reject template copy plus real request packet creation",
  "reject real request packet creation plus evidence artifact creation",
  "reject evidence artifact creation plus future evidence checker creation",
  "reject remote read-only validation plus staging migration execution",
  "reject staging migration execution plus source-depth production transition",
  "reject source-depth production transition plus scoreSource=real transition",
  "reject scoreSource=real transition plus public claims",
  "reject SQL execution plus public claims",
  "reject Supabase access plus public claims",
  "reject source-rights approval plus public claims",
  "future packet readiness requires one requested scope",
  "future packet readiness requires explicit non-scope",
  "future packet readiness requires owner signoff map",
  "future packet readiness requires stop conditions",
  "future packet readiness requires rollback boundary",
  "future packet readiness requires gate impact",
  "future packet readiness requires no market-data rows",
  "future packet readiness requires no external-system output",
  "future packet readiness requires no approval-like wording before CEO decision",
  "future packet readiness requires source-depth not_ready remains protected until explicitly approved",
  "CEO recommendation: use this rulebook as a pre-check before any future approval packet is drafted",
  "pre-checking single-scope rules prevents accidental bundled authorization",
  "Next safe slice: record CP3 source-depth local-only single-scope approval packet rulebook role review",
  "The next safe slice must remain local-only",
  "The next safe slice must not approve template copy",
  "The next safe slice must not create a real request packet",
  "The next safe slice must not create evidence files",
  "The next safe slice must not fill template values",
  "The next safe slice must not create the future evidence checker",
  "The next safe slice must not connect to Supabase",
  "The next safe slice must not run SQL",
  "The next safe slice must not fetch market data",
  "The next safe slice must not parse market rows",
  "The next safe slice must not wire runtime code",
  "The next safe slice must not set scoreSource=real",
  "The next safe slice must not clear source-depth not_ready",
  "The next safe slice must not make public claims",
  "scripts/check-cp3-source-depth-local-only-single-scope-approval-packet-rulebook.mjs passes",
  "scripts/check-cp3-source-depth-local-only-approval-packet-scope-map-role-review.mjs passes",
  "scripts/check-cp3-source-depth-local-only-approval-packet-scope-map.mjs passes",
  "scripts/check-cp3-tw-stock-source-depth.mjs remains not_ready as expected",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "single-scope approval packet rulebook only",
  "do not approve template copy",
  "do not create a real request packet",
  "do not create real evidence artifact files",
  "do not fill template values",
  "do not create future evidence checker",
  "do not add example market data",
  "do not add sample rows",
  "do not add sample JSON",
  "do not add sample CSV",
  "do not add Supabase output",
  "do not add SQL output",
  "do not fetch market data",
  "do not parse market rows",
  "do not run source-depth validator against Supabase",
  "do not connect to Supabase",
  "do not run SQL",
  "do not write Supabase",
  "do not write staging rows",
  "do not write daily_prices",
  "do not create seed SQL",
  "do not store raw market rows",
  "do not commit CSV / JSON market data files",
  "do not set scoreSource=real",
  "do not make public backtest claims",
  "do not clear source-depth not_ready",
  "CP3 source-depth production gate remains not_ready",
  "Keep public data source mock",
  "record CP3 source-depth local-only single-scope approval packet rulebook role review"
];

const forbiddenPhrases = [
  "Status: approved",
  "CEO Decision: APPROVE",
  "Approval Status: approved",
  "template copy approved",
  "real request packet approved",
  "evidence artifacts approved",
  "future evidence checker approved",
  "source_depth_state is reviewable",
  "scoreSource=real approved",
  "source-rights are approved",
  "public claims are approved",
  "Supabase read output:",
  "SQL execution output:",
  "raw OHLCV",
  "daily_prices sample"
];

const report = fs.existsSync(reportPath) ? fs.readFileSync(reportPath, "utf8") : "";
const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      report: reportPath,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
