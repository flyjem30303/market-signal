import fs from "node:fs";

const reportPath = "docs/reviews/CP3_SOURCE_DEPTH_LOCAL_ONLY_TRANSITION_BLOCKER_CHECKPOINT_SUMMARY_2026-05-29.md";

const requiredPhrases = [
  "Status: CP3 source-depth local-only transition blocker checkpoint summary recorded",
  "REVISE",
  "local-only transition blocker checkpoint summary",
  "completed local-only transition blocker governance chain",
  "does not approve template copy",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not fill template values",
  "does not create the future evidence checker",
  "does not clear source-depth not_ready",
  "transition readiness blocker index completed",
  "transition readiness blocker index role review completed",
  "transition blocker owner matrix completed",
  "transition blocker owner matrix role review completed",
  "review gates include the transition blocker governance chain",
  "source-depth production gate remains not_ready",
  "public data source remains mock",
  "blockers have explicit owners",
  "blockers have explicit evidence dependencies",
  "blockers have explicit stop conditions",
  "blockers have explicit blocked states",
  "CEO decision visibility is improved",
  "approval boundaries remain outside autonomous execution",
  "external systems remain outside autonomous execution",
  "runtime wiring remains outside autonomous execution",
  "public claims remain outside autonomous execution",
  "template-copy approval remains pending not approved",
  "real request packet creation remains pending not approved",
  "real evidence artifact creation remains pending not approved",
  "template value filling remains pending not approved",
  "future evidence checker creation remains pending not approved",
  "remote read-only validation remains pending not approved",
  "staging migration execution remains pending not approved",
  "source-depth production transition remains pending not approved",
  "scoreSource=real transition remains pending not approved",
  "public claims remain pending not approved",
  "stop if transition blocker checkpoint summary is treated as approval",
  "stop if transition blocker checkpoint summary is treated as executable task",
  "stop if transition blocker checkpoint summary is used to create a real request packet",
  "stop if transition blocker checkpoint summary is used to create evidence files",
  "stop if transition blocker checkpoint summary is used to fill template values",
  "stop if transition blocker checkpoint summary is used to create future evidence checker",
  "stop if transition blocker checkpoint summary is used to connect to Supabase",
  "stop if transition blocker checkpoint summary is used to run SQL",
  "stop if transition blocker checkpoint summary is used to fetch market data",
  "stop if transition blocker checkpoint summary is used to set scoreSource=real",
  "stop if transition blocker checkpoint summary is used to clear source-depth not_ready",
  "stop if transition blocker checkpoint summary is used to make public claims",
  "scripts/check-cp3-source-depth-local-only-transition-blocker-checkpoint-summary.mjs passes",
  "scripts/check-cp3-source-depth-local-only-transition-blocker-owner-matrix-role-review.mjs passes",
  "scripts/check-cp3-source-depth-local-only-transition-blocker-owner-matrix.mjs passes",
  "scripts/check-cp3-tw-stock-source-depth.mjs remains not_ready as expected",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "local-only progress reporting",
  "remaining CP3 source-depth transition blockers are indexed",
  "blockers are assigned owners",
  "blockers are tied to evidence dependencies",
  "blockers are tied to stop conditions",
  "blockers are preserved as blocked",
  "must not approve pending items",
  "must not convert pending items into work",
  "must not create request packets",
  "must not create evidence files",
  "must not fill template values",
  "must not create future checkers",
  "must not connect to Supabase",
  "must not run SQL",
  "must not fetch market data",
  "must not wire runtime code",
  "must not set scoreSource=real",
  "must not clear source-depth not_ready",
  "must not make public claims",
  "transition blocker checkpoint summary only",
  "do not approve template copy",
  "do not create a real request packet",
  "do not create real evidence artifact files",
  "do not fill template values",
  "do not create future evidence checker",
  "do not add example market data",
  "do not add sample rows",
  "do not add sample JSON",
  "do not add sample CSV",
  "do not add Supabase output",
  "do not add SQL output",
  "do not fetch market data",
  "do not parse market rows",
  "do not run source-depth validator against Supabase",
  "do not connect to Supabase",
  "do not run SQL",
  "do not write Supabase",
  "do not write staging rows",
  "do not write daily_prices",
  "do not create seed SQL",
  "do not store raw market rows",
  "do not commit CSV / JSON market data files",
  "do not set scoreSource=real",
  "do not make public backtest claims",
  "do not clear source-depth not_ready",
  "CP3 source-depth production gate remains not_ready",
  "Keep public data source mock",
  "record CP3 source-depth local-only transition blocker checkpoint summary role review"
];

const forbiddenPhrases = [
  "Status: approved",
  "CEO Decision: APPROVE",
  "Approval Status: approved",
  "template copy approved",
  "real request packet approved",
  "evidence artifacts approved",
  "future evidence checker approved",
  "source_depth_state is reviewable",
  "scoreSource=real approved",
  "source-rights are approved",
  "public claims are approved",
  "Supabase read output:",
  "SQL execution output:",
  "raw OHLCV",
  "daily_prices sample"
];

const report = fs.existsSync(reportPath) ? fs.readFileSync(reportPath, "utf8") : "";
const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      report: reportPath,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
