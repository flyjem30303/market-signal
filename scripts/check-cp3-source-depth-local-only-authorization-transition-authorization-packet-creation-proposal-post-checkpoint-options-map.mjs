import fs from "node:fs";

const reportPath =
  "docs/reviews/CP3_SOURCE_DEPTH_LOCAL_ONLY_AUTHORIZATION_TRANSITION_AUTHORIZATION_PACKET_CREATION_PROPOSAL_POST_CHECKPOINT_OPTIONS_MAP_2026-05-29.md";

const requiredPhrases = [
  "Status: CP3 source-depth local-only authorization transition authorization packet creation proposal post-checkpoint options map recorded",
  "REVISE",
  "local-only authorization transition authorization packet creation proposal post-checkpoint options map",
  "safe choices after the local-only authorization-transition-authorization packet creation proposal readiness and rejection governance chain",
  "does not approve authorization",
  "does not start an approval workflow",
  "does not create an authorization packet",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not fill template values",
  "does not create the future evidence checker",
  "does not clear source-depth not_ready",
  "Option A: continue local-only governance documentation",
  "Option B: prepare authorization transition decision dependency mapping without approving it",
  "Option C: map human authorization meeting agenda without starting approval workflow",
  "Option D: pause authorization-transition-authorization packet creation proposal path until human authorization",
  "Option E: reject any option that includes external-system execution",
  "continue local-only role review documentation",
  "continue local-only gate coverage documentation",
  "continue local-only authorization transition dependency mapping",
  "continue local-only rejection criteria mapping",
  "continue local-only stop-condition mapping",
  "continue local-only manifest and checkpoint maintenance",
  "continue local-only static checker coverage",
  "continue TypeScript noEmit verification",
  "continue review gate verification",
  "authorization approval requires human authorization",
  "approval workflow start requires human authorization",
  "authorization packet creation requires human authorization",
  "real request packet creation requires human authorization",
  "real evidence artifact creation requires human authorization",
  "template value filling requires human authorization",
  "future evidence checker creation requires human authorization",
  "remote read-only validation requires human authorization",
  "staging migration execution requires human authorization",
  "source-depth production transition requires human authorization",
  "scoreSource=real transition requires human authorization",
  "public claims require human authorization",
  "reject autonomous authorization approval",
  "reject autonomous approval workflow start",
  "reject autonomous authorization packet creation",
  "reject autonomous real request packet creation",
  "reject autonomous real evidence artifact creation",
  "reject autonomous template value filling",
  "reject autonomous future evidence checker creation",
  "reject autonomous Supabase connection",
  "reject autonomous SQL execution",
  "reject autonomous market data fetch",
  "reject autonomous market row parsing",
  "reject autonomous staging row writes",
  "reject autonomous daily_prices writes",
  "reject autonomous seed SQL creation",
  "reject autonomous scoreSource=real transition",
  "reject autonomous source-depth not_ready clearance",
  "reject autonomous public claim creation",
  "authorization approval depends on human authorization",
  "approval workflow start depends on human authorization",
  "authorization packet creation depends on human authorization",
  "source-depth production transition depends on human authorization",
  "scoreSource=real transition depends on human authorization",
  "remote read-only validation depends on human authorization",
  "staging migration execution depends on human authorization",
  "public claims depend on human authorization",
  "source-rights confirmation depends on human authorization",
  "external-system output remains outside autonomous execution",
  "market-data rows remain outside autonomous execution",
  "runtime wiring remains outside autonomous execution",
  "stop if options map is treated as authorization approval",
  "stop if options map is treated as approval workflow start",
  "stop if options map is treated as executable task",
  "stop if options map is used to create an authorization packet",
  "stop if options map is used to create a real request packet",
  "stop if options map is used to create evidence files",
  "stop if options map is used to fill template values",
  "stop if options map is used to create future evidence checker",
  "stop if options map is used to connect to Supabase",
  "stop if options map is used to run SQL",
  "stop if options map is used to fetch market data",
  "stop if options map is used to parse market rows",
  "stop if options map is used to wire runtime code",
  "stop if options map is used to set scoreSource=real",
  "stop if options map is used to clear source-depth not_ready",
  "stop if options map is used to make public claims",
  "scripts/check-cp3-source-depth-local-only-authorization-transition-authorization-packet-creation-proposal-post-checkpoint-options-map.mjs passes",
  "scripts/check-cp3-source-depth-local-only-authorization-transition-authorization-packet-creation-proposal-rejection-gate-checkpoint-summary-role-review.mjs passes",
  "scripts/check-cp3-source-depth-local-only-authorization-transition-authorization-packet-creation-proposal-rejection-gate-checkpoint-summary.mjs passes",
  "scripts/check-cp3-tw-stock-source-depth.mjs remains not_ready as expected",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "keeps the project moving without crossing into authorization approval",
  "keeps approval workflow start outside autonomous work",
  "keeps authorization packet creation outside autonomous work",
  "keeps data execution outside autonomous work",
  "keeps external-system execution outside autonomous work",
  "keeps runtime execution outside autonomous work",
  "keeps public-claim execution outside autonomous work",
  "safest next autonomous path is local-only authorization transition decision dependency mapping and role review coverage",
  "authorization approval remains blocked until human authorization",
  "approval workflow start remains blocked until human authorization",
  "authorization packet creation remains blocked until human authorization",
  "real packet creation remains blocked until human authorization",
  "filled values remain blocked until human authorization",
  "evidence creation remains blocked until human authorization",
  "Supabase connection remains blocked until human authorization",
  "SQL execution remains blocked until human authorization",
  "market data fetch remains blocked until human authorization",
  "market row parsing remains blocked until human authorization",
  "scoreSource=real remains blocked until human authorization",
  "source-depth not_ready clearance remains blocked until human authorization",
  "public claims remain blocked until human authorization",
  "authorization transition authorization packet creation proposal post-checkpoint options map only",
  "do not approve authorization",
  "do not start an approval workflow",
  "do not create authorization packet",
  "do not create a real request packet",
  "do not create real evidence artifact files",
  "do not fill template values",
  "do not create future evidence checker",
  "do not add example market data",
  "do not add sample rows",
  "do not add sample JSON",
  "do not add sample CSV",
  "do not add Supabase output",
  "do not add SQL output",
  "do not fetch market data",
  "do not parse market rows",
  "do not run source-depth validator against Supabase",
  "do not connect to Supabase",
  "do not run SQL",
  "do not write Supabase",
  "do not write staging rows",
  "do not write daily_prices",
  "do not create seed SQL",
  "do not store raw market rows",
  "do not commit CSV / JSON market data files",
  "do not set scoreSource=real",
  "do not make public backtest claims",
  "do not clear source-depth not_ready",
  "CP3 source-depth production gate remains not_ready",
  "Keep public data source mock",
  "record CP3 source-depth local-only authorization transition authorization packet creation proposal post-checkpoint options map role review"
];

const forbiddenPhrases = [
  "Status: approved",
  "CEO Decision: APPROVE",
  "Approval Status: approved",
  "authorization approved",
  "approval workflow started",
  "authorization packet created",
  "real request packet approved",
  "evidence artifacts approved",
  "future evidence checker approved",
  "source_depth_state is reviewable",
  "scoreSource=real approved",
  "source-rights are approved",
  "public claims are approved",
  "Supabase read output:",
  "SQL execution output:",
  "raw OHLCV",
  "daily_prices sample"
];

const report = fs.existsSync(reportPath) ? fs.readFileSync(reportPath, "utf8") : "";
const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      report: reportPath,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
