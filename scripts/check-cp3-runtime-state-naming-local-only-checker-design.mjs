import fs from "node:fs";

const reportPath = "docs/reviews/CP3_RUNTIME_STATE_NAMING_LOCAL_ONLY_CHECKER_DESIGN_2026-05-30.md";
const report = fs.readFileSync(reportPath, "utf8");

const requiredPhrases = [
  "Status: CP3 runtime state naming local-only checker design recorded",
  "PROCEED",
  "future local-only static checker design",
  "before runtime implementation begins",
  "does not implement\nruntime state naming",
  "does not scan production runtime files",
  "does not wire UI",
  "does not connect to Supabase",
  "does not run SQL",
  "does not fetch market data",
  "does\nnot parse market rows",
  "does not set scoreSource=real",
  "does not clear\nsource-depth not_ready",
  "does not make public claims",
  "does not approve authorization",
  "does not schedule a formal meeting",
  "does not create an authorization packet",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not\nwrite Supabase",
  "does not write staging rows",
  "does not write daily_prices",
  "does\nnot create seed SQL",
  "does not wire runtime code",
  "CHECKER-SCOPE-001 checker design validates local-only documentation content",
  "CHECKER-SCOPE-002 checker design records allowed state names",
  "CHECKER-SCOPE-003 checker design records rejected state names",
  "CHECKER-SCOPE-004 checker design records required blocked-state semantics",
  "CHECKER-SCOPE-005 checker design records future source-contract alignment expectations",
  "CHECKER-SCOPE-006 checker design records runtime implementation stop conditions",
  "CHECKER-OUT-001 checker design does not scan runtime code",
  "CHECKER-OUT-002 checker design does not import app modules",
  "CHECKER-OUT-003 checker design does not inspect Supabase data",
  "CHECKER-OUT-004 checker design does not validate market data",
  "CHECKER-OUT-005 checker design does not assert public release readiness",
  "CHECKER-OUT-006 checker design does not approve scoreSource=real",
  "FUTURE-CHECK-001 required allowed states: mock, internal_review, blocked, partial, stale, unavailable, approved, unknown",
  "FUTURE-CHECK-002 required rejected states: real_ready, production_ready, verified, trusted, official, live, accurate, complete, safe, recommended",
  "FUTURE-CHECK-003 required blocked semantics: missing legal approval maps to blocked",
  "FUTURE-CHECK-004 required blocked semantics: missing source-depth approval maps to blocked",
  "FUTURE-CHECK-005 required blocked semantics: missing authorization maps to blocked",
  "FUTURE-CHECK-006 required stale semantics: unknown freshness maps to stale or unavailable",
  "FUTURE-CHECK-007 required partial semantics: incomplete fields, coverage, validation, or criteria map to partial or unavailable",
  "FUTURE-CHECK-008 required fallback semantics: missing required state source fields map to unavailable",
  "FUTURE-CHECK-009 required approved semantics: approved requires all approval states and separately authorized scoreSource=real",
  "FUTURE-CHECK-010 required public boundary: internal_review cannot be exposed as public reliability wording",
  "FUTURE-CHECK-011 required public boundary: unknown cannot be upgraded into trusted wording",
  "FUTURE-CHECK-012 required advice boundary: no state name can imply personalized advice, prediction, recommendation, or performance assurance",
  "FORBIDDEN-CHECKER-OUTCOME-001 checker must not report runtime implementation ready",
  "FORBIDDEN-CHECKER-OUTCOME-002 checker must not report UI wiring ready",
  "FORBIDDEN-CHECKER-OUTCOME-003 checker must not report scoreSource=real ready",
  "FORBIDDEN-CHECKER-OUTCOME-004 checker must not report source-depth not_ready cleared",
  "FORBIDDEN-CHECKER-OUTCOME-005 checker must not report public claims approved",
  "FORBIDDEN-CHECKER-OUTCOME-006 checker must not report Supabase validation complete",
  "FORBIDDEN-CHECKER-OUTCOME-007 checker must not report SQL execution complete",
  "FORBIDDEN-CHECKER-OUTCOME-008 checker must not report market data validation complete",
  "CHECKER-INPUT-001 future checker may read local docs and local config only",
  "CHECKER-INPUT-002 future checker may read allowlist and denylist text only",
  "CHECKER-INPUT-003 future checker may read sample local-only state packet text only after separate approval",
  "CHECKER-INPUT-004 future checker must not read raw market data files",
  "CHECKER-INPUT-005 future checker must not read .env.local",
  "CHECKER-INPUT-006 future checker must not connect to network",
  "CHECKER-INPUT-007 future checker must not import runtime app code",
  "CHECKER-INPUT-008 future checker must not use Supabase client",
  "IMPLEMENTATION-BOUNDARY-001 creating this checker design is allowed",
  "IMPLEMENTATION-BOUNDARY-002 creating a future local-only checker script for documentation is allowed",
  "IMPLEMENTATION-BOUNDARY-003 creating a future runtime implementation plan is allowed",
  "IMPLEMENTATION-BOUNDARY-004 wiring state names into app code is not allowed here",
  "IMPLEMENTATION-BOUNDARY-005 exposing state names in public UI is not allowed here",
  "IMPLEMENTATION-BOUNDARY-006 reading real state packets is not allowed here",
  "IMPLEMENTATION-BOUNDARY-007 approving scoreSource=real is not allowed here",
  "IMPLEMENTATION-BOUNDARY-008 clearing source-depth not_ready is not allowed here",
  "CEO stage direction: continue local-only preparation until the next action would implement runtime state naming",
  "CEO stage direction: complete checker design before implementation boundary report",
  "CEO stage direction: next stage item should be runtime pre-implementation boundary checklist",
  "CEO stage direction: report stage work items after the pre-implementation boundary checklist is recorded",
  "Next safe slice: prepare CP3 runtime state naming pre-implementation boundary checklist",
  "Alternative next safe slice: prepare CP3 runtime state naming role review only if CEO wants additional scrutiny",
  "CEO recommendation: prepare CP3 runtime state naming pre-implementation boundary checklist",
  "The next safe slice must remain local-only",
  "The next safe slice must not implement runtime data state naming",
  "The next safe slice must not wire UI",
  "The next safe slice must not connect to Supabase",
  "The next safe slice must not run SQL",
  "The next safe slice must not fetch market data",
  "The next safe slice must not parse market rows",
  "The next safe slice must not set scoreSource=real",
  "The next safe slice must not clear source-depth not_ready",
  "The next safe slice must not make public claims",
  "scripts/check-cp3-runtime-state-naming-local-only-checker-design.mjs passes",
  "scripts/check-cp3-runtime-state-naming-acceptance-criteria.mjs passes",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "public data source remains mock",
  "CP3 source-depth production gate remains not_ready",
  "scoreSource=real remains blocked",
  "Supabase and SQL execution remain blocked"
];

const forbiddenPhrases = [
  "authorization is approved",
  "authorization has been approved",
  "formal meeting is scheduled",
  "authorization packet is created",
  "real request packet is created",
  "real evidence artifact files are created",
  "Supabase connection is allowed",
  "SQL execution is allowed",
  "market data fetch is allowed",
  "market rows are parsed",
  "staging rows are written",
  "daily_prices rows are written",
  "seed SQL is created",
  "runtime code is wired",
  "runtime state naming is implemented",
  "scoreSource=real approved",
  "source-depth production gate is ready",
  "public data source is real",
  "public claims are approved for release",
  "release readiness is approved"
];

const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
