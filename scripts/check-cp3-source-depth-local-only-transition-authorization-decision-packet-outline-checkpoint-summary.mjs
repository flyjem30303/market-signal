import fs from "node:fs";

const reportPath =
  "docs/reviews/CP3_SOURCE_DEPTH_LOCAL_ONLY_TRANSITION_AUTHORIZATION_DECISION_PACKET_OUTLINE_CHECKPOINT_SUMMARY_2026-05-29.md";

const requiredPhrases = [
  "Status: CP3 source-depth local-only transition authorization decision packet outline checkpoint summary recorded",
  "REVISE",
  "local-only transition authorization decision packet outline checkpoint summary",
  "completed decision-packet outline planning chain",
  "does not approve authorization",
  "does not start an approval workflow",
  "does not create an authorization packet",
  "does not create a real request packet",
  "does not create real evidence artifact files",
  "does not fill template values",
  "does not create the future evidence checker",
  "does not clear source-depth not_ready",
  "transition authorization checkpoint summary completed",
  "transition authorization checkpoint summary role review completed",
  "transition authorization post-checkpoint options map completed",
  "transition authorization post-checkpoint options map role review completed",
  "transition authorization decision packet outline completed",
  "transition authorization decision packet outline role review completed",
  "review gates include the transition authorization decision packet outline chain",
  "source-depth production gate remains not_ready",
  "public data source remains mock",
  "scoreSource=real remains blocked",
  "future decision packet sections are explicitly named",
  "future decision slots are explicitly named",
  "future evidence slots are explicitly named",
  "future stop conditions are explicitly named",
  "role concerns are reconciled before any approval workflow",
  "CEO decision visibility is improved",
  "authorization remains outside autonomous execution",
  "external systems remain outside autonomous execution",
  "runtime wiring remains outside autonomous execution",
  "public claims remain outside autonomous execution",
  "authorization decision remains pending not approved",
  "approval workflow start remains pending not approved",
  "authorization packet creation remains pending not approved",
  "real request packet creation remains pending not approved",
  "real evidence artifact creation remains pending not approved",
  "template value filling remains pending not approved",
  "future evidence checker creation remains pending not approved",
  "remote read-only validation remains pending not approved",
  "Supabase connection remains pending not approved",
  "SQL execution remains pending not approved",
  "staging migration execution remains pending not approved",
  "source-rights acceptance remains pending not approved",
  "source-depth production transition remains pending not approved",
  "scoreSource=real transition remains pending not approved",
  "public claims remain pending not approved",
  "stop if decision packet outline checkpoint summary is treated as authorization",
  "stop if decision packet outline checkpoint summary is treated as approval workflow start",
  "stop if decision packet outline checkpoint summary is treated as executable task",
  "stop if decision packet outline checkpoint summary is used to create an authorization packet",
  "stop if decision packet outline checkpoint summary is used to create a real request packet",
  "stop if decision packet outline checkpoint summary is used to create evidence files",
  "stop if decision packet outline checkpoint summary is used to fill template values",
  "stop if decision packet outline checkpoint summary is used to create future evidence checker",
  "stop if decision packet outline checkpoint summary is used to connect to Supabase",
  "stop if decision packet outline checkpoint summary is used to run SQL",
  "stop if decision packet outline checkpoint summary is used to fetch market data",
  "stop if decision packet outline checkpoint summary is used to parse market rows",
  "stop if decision packet outline checkpoint summary is used to wire runtime code",
  "stop if decision packet outline checkpoint summary is used to set scoreSource=real",
  "stop if decision packet outline checkpoint summary is used to clear source-depth not_ready",
  "stop if decision packet outline checkpoint summary is used to make public claims",
  "scripts/check-cp3-source-depth-local-only-transition-authorization-decision-packet-outline-checkpoint-summary.mjs passes",
  "scripts/check-cp3-source-depth-local-only-transition-authorization-decision-packet-outline-role-review.mjs passes",
  "scripts/check-cp3-source-depth-local-only-transition-authorization-decision-packet-outline.mjs passes",
  "scripts/check-cp3-tw-stock-source-depth.mjs remains not_ready as expected",
  "scripts/check-review-gates.mjs passes",
  "TypeScript noEmit passes",
  "local-only progress reporting",
  "future authorization decision packet structure has been outlined",
  "future authorization decision packet structure has been reviewed by roles",
  "future authorization decision packet structure is tied to stop conditions",
  "future authorization decision packet structure is preserved as non-authorizing planning",
  "must not approve authorization",
  "must not start an approval workflow",
  "must not create authorization packets",
  "must not create request packets",
  "must not create evidence files",
  "must not fill template values",
  "must not create future checkers",
  "must not connect to Supabase",
  "must not run SQL",
  "must not fetch market data",
  "must not parse market rows",
  "must not wire runtime code",
  "must not set scoreSource=real",
  "must not clear source-depth not_ready",
  "must not make public claims",
  "transition authorization decision packet outline checkpoint summary only",
  "do not approve authorization",
  "do not start an approval workflow",
  "do not create authorization packet",
  "do not create a real request packet",
  "do not create real evidence artifact files",
  "do not fill template values",
  "do not create future evidence checker",
  "do not add example market data",
  "do not add sample rows",
  "do not add sample JSON",
  "do not add sample CSV",
  "do not add Supabase output",
  "do not add SQL output",
  "do not fetch market data",
  "do not parse market rows",
  "do not run source-depth validator against Supabase",
  "do not connect to Supabase",
  "do not run SQL",
  "do not write Supabase",
  "do not write staging rows",
  "do not write daily_prices",
  "do not create seed SQL",
  "do not store raw market rows",
  "do not commit CSV / JSON market data files",
  "do not set scoreSource=real",
  "do not make public backtest claims",
  "do not clear source-depth not_ready",
  "CP3 source-depth production gate remains not_ready",
  "Keep public data source mock",
  "record CP3 source-depth local-only transition authorization decision packet outline checkpoint summary role review"
];

const forbiddenPhrases = [
  "Status: approved",
  "CEO Decision: APPROVE",
  "Approval Status: approved",
  "authorization approved",
  "approval workflow started",
  "authorization packet created",
  "real request packet approved",
  "evidence artifacts approved",
  "future evidence checker approved",
  "source_depth_state is reviewable",
  "scoreSource=real approved",
  "source-rights are approved",
  "public claims are approved",
  "Supabase read output:",
  "SQL execution output:",
  "raw OHLCV",
  "daily_prices sample"
];

const report = fs.existsSync(reportPath) ? fs.readFileSync(reportPath, "utf8") : "";
const missing = requiredPhrases.filter((phrase) => !report.includes(phrase));
const forbidden = forbiddenPhrases.filter((phrase) => report.includes(phrase));

console.log(
  JSON.stringify(
    {
      forbidden,
      missing,
      report: reportPath,
      status: missing.length === 0 && forbidden.length === 0 ? "ok" : "blocked"
    },
    null,
    2
  )
);

if (missing.length > 0 || forbidden.length > 0) {
  process.exitCode = 1;
}
